# ==========================================================
# 음향 스펙트로그램 기반 이미지 생성기 - 필요 라이브러리
# 설치 방법: pip install -r requirements.txt
# ==========================================================

# --- AI 모델 (Stable Diffusion) ---
torch>=2.0.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0

# --- 웹 UI ---
gradio>=4.0.0

# --- 오디오 처리 ---
librosa>=0.10.0
soundfile>=0.12.1        # librosa 오디오 로딩 백엔드 (wav 등)

# --- 시각화 / 데이터 / 이미지 ---
matplotlib>=3.7.0
numpy>=1.24.0
pillow>=10.0.0